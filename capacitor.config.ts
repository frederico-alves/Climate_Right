import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Climate_Right',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
